# MOD-05-讲者模式

## 文档信息
- **项目名称**: Web PPT
- **文档编号**: MOD-05
- **版本**: v1.0
- **对应PRD**: FR-008, US-005

---

## 1. 系统定位

### 在整体架构中的位置

**所属层次**: L4-L5（接口层 + 展示层）

**架构定位图**:
```
┌─────────────────────────────────────────────────────┐
│              ★ 讲者模式模块 (MOD-05) ★             │
│           SpeakerView | Timer | NotesView          │
└─────────────────────────────────────────────────────┘
                        │ ▼ 依赖
┌─────────────────────────────────────────────────────┐
│                   基础设施层 (L1)                    │
│              Reveal.js Speaker View               │
└─────────────────────────────────────────────────────┘
```

### 核心职责

- **演讲者视图**: 当前页+下一页预览+计时器+备注

---

## 2. 对应PRD

| PRD章节 | 编号 | 内容 |
|---------|-----|------|
| 功能需求 | FR-008 | 讲者模式 |
| 用户故事 | US-005 | 讲者模式 |
| 验收标准 | AC-005-01 ~ AC-005-04 | 讲者验收标准 |

---

## 3. 核心设计

### 3.1 讲者视图布局

```
┌────────────────────────────────────────────┐
│  当前页预览              │  下一页预览       │
│  ┌────────────────┐     │  ┌────────────┐ │
│  │                │     │  │            │ │
│  │   当前幻灯片   │     │  │ 下一张预览  │ │
│  │                │     │  │            │ │
│  └────────────────┘     │  └────────────┘ │
├────────────────────────────────────────────┤
│  00:15:30          │  演讲者备注区域        │
│  [开始] [暂停] [重置]│  ...                 │
└────────────────────────────────────────────┘
```

### 3.2 计时器功能

```typescript
interface TimerState {
  elapsed: number;        // 已用时间（秒）
  isRunning: boolean;    // 是否运行中
  isPaused: boolean;     // 是否暂停
}

function useTimer() {
  const state = reactive<TimerState>({
    elapsed: 0,
    isRunning: false,
    isPaused: false
  });

  const start = () => { /* ... */ };
  const pause = () => { /* ... */ };
  const reset = () => { /* ... */ };

  return { state, start, pause, reset };
}
```

---

## 4. 验收标准

| 标准 | 要求 | 验证方法 |
|-----|------|---------|
| AC-005-01 | 支持演讲者视图窗口 | UI测试 |
| AC-005-02 | 显示当前页和下一页预览 | UI测试 |
| AC-005-03 | 演讲计时器功能 | 功能测试 |
| AC-005-04 | 显示当前幻灯片备注 | 功能测试 |

---

## 5. 覆盖映射

| PRD类型 | PRD编号 | 架构元素 | 覆盖状态 |
|---------|---------|---------|---------|
| 功能需求 | FR-008 | SpeakerView | ✅ |
| 用户故事 | US-005 | Timer + NotesView | ✅ |
| 验收标准 | AC-005-01~04 | 组件实现 | ✅ |

---

## 变更历史

| 版本 | 日期 | 变更内容 |
|-----|------|---------|
| 1.0 | 2026-03-06 | 初始版本 |
